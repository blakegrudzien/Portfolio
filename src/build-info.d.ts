/** ISO timestamp of the commit this build was made from, injected by the
 * `define` in vite.config.ts. Used by the footer to say when the site was
 * last changed. */
declare const __LAST_UPDATED__: string
